[package]
name = "game-ttt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
mcts = { path = "../../mcts" }
game-core = { path = "../game-core" }
game-host = { path = "../../game-host" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
proptest = "1.4.0"

[dev-dependencies]
rustc-hash = "1.1.0"