[package]
name = "pyramid"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bitboard = { path = "../bitboard" }
rustc-hash = "1.1.0"

[dev-dependencies]
proptest = "1.4.0"

[lints]
workspace = true
