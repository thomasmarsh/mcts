/target
plan/
Cargo.lock
__pycache__
optuna_output
/profile.json
.DS_Store
node_modules
# Python virtual environment (uv project)
tuner/.venv/
# setuptools build metadata
*.egg-info/
# pnpm workspace (ui/)
ui/node_modules/
ui/pnpm-lock.yaml
# Vite build output (served by the cargo server's ServeDir)
server/static/dist/
AGENTS.md
CLAUDE.md
.claude
bench-runs/
# Transient background-job logs and working files
scratchpad/
# Generated opening-book files (examples/build_book.rs and friends) --
# regenerate with the relevant game's `build_book` example rather than
# committing what's essentially large, easily-reproducible binary data.
books/

# Per-machine Cargo build configuration (machine-specific paths like
# DUCKDB_LIB_DIR, DUCKDB_INCLUDE_DIR for Homebrew-installed DuckDB)
.cargo/config.toml

