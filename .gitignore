/target
/local/
Cargo.lock
__pycache__
optuna_output
/profile.json
.DS_Store
node_modules
# Python virtual environment (uv project)
tools/tuner/.venv/
research/othello-eval/.venv/
research/az-train/.venv/
# setuptools build metadata
*.egg-info/
# pnpm workspace (ui/)
ui/node_modules/
.pnpm-store/
apps/ui/pnpm-lock.yaml
# Vite build output (served by the cargo server's ServeDir)
apps/server/static/dist/
AGENTS.md
CLAUDE.md
.claude

# Vendored Edax build (games/othello/edax/build-edax.sh regenerates it) --
# a third-party C engine used only as an external Othello strength yardstick.
games/othello/edax/vendor/

# Per-machine Cargo build configuration (machine-specific paths like
# DUCKDB_LIB_DIR, DUCKDB_INCLUDE_DIR for Homebrew-installed DuckDB)
.cargo/config.toml
